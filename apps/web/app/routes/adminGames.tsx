/** Canonical entry point for the combined game administration workspace.
 *
 * `/admin/sandbox-games` keeps loading the same route module for old bookmarks, while all current
 * navigation and documentation point to `/admin/games`.
 */
export { default, meta } from "./adminSandboxGames";
